# CF-VPN Environment Configuration
# Values here become BuildConfig fields — must be valid Java string literals.
# For CI, the workflow writes .env with actual defaults.
KEYSTORE_PATH=""
STORE_PASSWORD="android"
KEY_PASSWORD="android"
